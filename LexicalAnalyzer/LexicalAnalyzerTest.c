#include<stdio.h>
#define zz 5.63

void main()
{  
    char x[5], y='A';
    int $a6, 5xy, a, b, c, d, i=0;
    a = 1; //this is a single line comment
    b = 10;
    c = 12.3;
    a *= b;
    b += a/c - 1;
    c -= a+b%d;
    for(i=0;i<5;i++)
        {
        int a=i*2;
        a=a^i;
        scanf("%c",&x[i]);
        }
    while(i>=0&&i!=10)
        {
        printf("d");
        i--;
/*
this is multiline comment to test the functionality of the analyzer
*/  
        printf("Sum: %d",c);
        }
    check(int a);

}